import numpy as np
from matplotlib import pyplot as pt

class PlotData():
    def plot(self,cntVec, countgeneration, saveIndGeneration, muVec, rVec, VerboseModuloGen, N, eigVals, r, P_empVecAll, P_empVecWindow, P_empAll, P_empWindow):
        #TODO: Implement plot of the Data that occurs in every iteration of generation While Loop
        return 0
